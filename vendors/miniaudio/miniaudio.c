/*
 * Single implementation unit for miniaudio. The header expands to roughly four megabytes,
 * so it is compiled exactly once here. Feature set is trimmed in CMakeLists.txt.
 */

#include "miniaudio.h"
